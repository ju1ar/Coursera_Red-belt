#include "test_runner.h"


template <class T>
ostream& operator << (ostream& os, const vector<T>& s) {
  os << "{";
  bool first = true;
  for (const auto& x : s) {
    if (!first) {
      os << ", ";
    }
    first = false;
    os << x;
  }
  return os << "}";
}

template <class T>
ostream& operator << (ostream& os, const set<T>& s) {
  os << "{";
  bool first = true;
  for (const auto& x : s) {
    if (!first) {
      os << ", ";
    }
    first = false;
    os << x;
  }
  return os << "}";
}

template <class K, class V>
ostream& operator << (ostream& os, const map<K, V>& m) {
  os << "{";
  bool first = true;
  for (const auto& kv : m) {
    if (!first) {
      os << ", ";
    }
    first = false;
    os << kv.first << ": " << kv.second;
  }
  return os << "}";
}

template<class T, class U>
void AssertEqual(const T& t, const U& u, const string& hint ) {
  if (t != u) {
    ostringstream os;
    os << "Assertion failed: " << t << " != " << u;
    if (!hint.empty()) {
      os << " hint: " << hint;
    }
    throw runtime_error(os.str());
  }
}

/* void AssertEqual(const string& t, const string & u, const string& hint ) { */
/*   if (t != u) { */
/*     ostringstream os; */
/*     os << "Assertion failed: " << t << " != " << u; */
/*     if (!hint.empty()) { */
/*       os << " hint: " << hint; */
/*     } */
/*     throw runtime_error(os.str()); */
/*   } */
/* } */

void Assert(bool b, const string& hint) {
  AssertEqual(b, true, hint);
}

